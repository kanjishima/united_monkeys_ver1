{
  "name": "chat-example",
  "version": "0.0.0",
  "description": "A chat example to showcase how to use `socket.io` with a static `express` server with `async` for control flow.",
  "main": "server.js",
  "repository": "",
  "author": "Mostafa Eweda <mo.eweda@gmail.com>",
  "dependencies": {
    "async": "~0.2.8",
    "express": "~3.2.4",
    "socket.io": "~0.9.14"
  }
}
//ejs
//mongodb
//mongod
//nodemon
//mongoose

//mongod --nojournal --noprealloc --dbpath container/models/db
//mongod --nojournal
//mongod --smallfiles

//for DB on 1terminal, for SERVER on 1terminal, for noSQL 1terminal. Recomended 3terminals for dev.

//Server On
//$ cd container/controllers
//$ nodemon app.js

//server.listen(process.env.PORT, process.env.IP);